package javaScriptExecutor;

import org.openqa.selenium.JavascriptExecutor;

public class JSTheory {

	
	/*1. Java Script Executor i.e JSE is separate interface parallel to the WebDriver
	 * 2. Now JSE having some methods that we have to use it with WebDriver reference
	 * 3. To achieve that have to do "Interface to Interface" Top casting
	 * 4.All browser understands the javaScript
	 * 5.Some good features are available in JSE that why we are using the JSE
	 * 
	 * 6.JSE is an interface
	 *  Java script code----> convert into Java 
	 *  
	 *  JavascriptExecutor js=(JavascriptExecutor)driver;---this is Interface casting without impacting the driver 
	 * Now js is the object reference variable having all JSE Interface realted methods
	 * 
	 * Note- if user enter wrong JS Script it will return "Null Pointer Exception"
	 * 
	 * 
	 */
	
	
	
}
